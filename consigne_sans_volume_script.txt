Si on enleve le volume alors :

RUN apt-get update && apt-get install -y \
    python3 \
    python3-venv \
    python3-pip \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /var/www/html

COPY . /var/www/html

RUN python3 -m venv /opt/mnemosyne-venv \
    && /opt/mnemosyne-venv/bin/pip install --upgrade pip \
    && /opt/mnemosyne-venv/bin/pip install mysql-connector-python

    dans le fichier Dockerfile